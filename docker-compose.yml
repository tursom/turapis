services:
  turapis:
    build: .
    container_name: turapis
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - TZ=Asia/Shanghai
    command:
      - "-addr"
      - ":8080"
      - "-db"
      - "/data/turapis.db"
      - "-static-dir"
      - "/static"
      - "-log-file"
      - "/data/turapis.log"
